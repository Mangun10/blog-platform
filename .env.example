# Database Configuration (Supabase PostgreSQL)
# IMPORTANT: URL-encode special characters in password (e.g., / = %2F, # = %23, @ = %40)

# Connection pooler for queries (port 6543) - use Transaction pooler from Supabase
DATABASE_URL="postgresql://postgres.xxxxx:[YOUR-PASSWORD]@aws-0-region.pooler.supabase.com:6543/postgres?pgbouncer=true&connection_limit=1"

# Direct connection for migrations (port 5432)
DIRECT_URL="postgresql://postgres.xxxxx:[YOUR-PASSWORD]@aws-0-region.pooler.supabase.com:5432/postgres"

# Server Configuration
PORT=3000
NODE_ENV=production

# Email Configuration (Optional - for newsletter features)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password
EMAIL_FROM=noreply@yourblog.com
